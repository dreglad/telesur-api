# Service name must be specified in HTTP header namse 'X-Service-Name'

query {

  # Current service
  service {
    id
    name
    language
    url
    users {
      id
      email
    }
    liveUrl
    videoUrl
    videoRestUrl
  }

}