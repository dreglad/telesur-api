# import Query.*, Service from './video/schema.graphql'
# import Query.* from './news/schema.graphql'

type Query {
  """Get a list of available services"""
  services: [Service!]!
  """Retreive a single service"""
  service(id: ID!): Service
}
