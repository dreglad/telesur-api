# import Query.* from './video/schema.graphql'
# import Query.* from './news/schema.graphql'

type Query {

  """Get a list of available services"""
  services: [Service!]!

  """Retreive a single service wither by name or id"""
  service(name: String, id: ID): Service

}
